# Example environment variables for notes_backend
# SECRET_KEY should be set via environment in production
# DEBUG toggles debug mode (True/False)
# ALLOWED_HOSTS is a comma-separated list of hosts
SECRET_KEY=replace-me-in-production
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1,testserver
